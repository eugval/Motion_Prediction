### TODO:
- Refactor file system  into folder structure
- Fix bug with distance
- remake the plotting script
- add the eval training to the idx sets
- Make generalised Unet implementation
- Allow grid search of hyperparameters

- Read papers on IoU metric
- put all the file paths into global variables or functions
- add more data augmentation (illumination, contrast color jitter)
- make globals with all the strings used throughout